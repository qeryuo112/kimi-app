# 需求影响分析（2026-05-31）

## 需求
1. 移除导入文档出题功能
2. 移除登录功能（本地运行，无需登录）

---

## 一、移除登录功能

### 影响范围（20 个文件）

**后端代码**
- `api/kimi/auth.ts` — OAuth 回调、token 交换、JWT 验证、请求认证
- `api/kimi/session.ts` — session token 签发/验证
- `api/kimi/platform.ts` — Kimi Open Platform 用户资料获取
- `api/kimi/types.ts` — OAuth 类型定义
- `api/auth-router.ts` — `logout` mutation 需删除，`me` 可保留或简化
- `api/context.ts` — 当前已有 LOCAL_USER 兜底，可进一步简化移除 authenticateRequest
- `api/boot.ts` — 移除 `/api/oauth/callback` 路由、bodyLimit cookie 逻辑可保留
- `api/lib/cookies.ts` — 若彻底移除 cookie 会话则不再需要
- `contracts/constants.ts` — 可移除 `Paths.oauthCallback`、`Session` 相关

**前端代码**
- `src/pages/Login.tsx` — 整个页面删除
- `src/hooks/useAuth.ts` — 当前调用 `trpc.auth.me` 和 `trpc.auth.logout`，需简化
- `src/components/Layout.tsx` — 移除 logout 按钮、简化用户信息显示
- `src/App.tsx` — 移除 `/login` 路由
- `src/const.ts` — 移除 `LOGIN_PATH`
- `src/pages/Home.tsx`、`Subjects.tsx`、`Skills.tsx` 等共 10 个页面 — 均使用 `useAuth({ redirectOnUnauthenticated: true })`，需移除 redirect 逻辑或让 hook 始终返回已登录

### 可选方案

**方案 A：彻底删除**
- 删除 `api/kimi/` 目录、Login 页面、logout 功能、cookie 相关代码
- `useAuth` 改为始终返回硬编码本地用户（不调用后端）
- `context.ts` 直接返回 LOCAL_USER，不再尝试认证
- **优点**：代码最干净，无冗余
- **缺点**：以后如果要恢复登录需重新写大量代码；前端 10 个页面的 `useAuth` 调用需调整

**方案 B：保留骨架但去功能化（推荐）**
- 删除 `api/kimi/` 目录、Login 页面、logout 功能
- 保留 `api/auth-router.ts` 的 `me` 查询（直接返回 LOCAL_USER）
- 保留 `useAuth` hook 结构，但让它始终返回已登录状态（仍可调 `auth.me` 获取本地用户）
- `context.ts` 直接返回 LOCAL_USER，不再调用 `authenticateRequest`
- **优点**：前端 10 个页面几乎无需改动，保留用户系统扩展性
- **缺点**：auth router 成了一个简单的本地用户存根

**方案 C：仅删除前端入口**
- 删除 Login 页面和 logout 按钮
- 保留后端所有认证代码
- **优点**：改动最小
- **缺点**：后端留着大量死代码，不够干净

---

## 二、移除导入文档出题功能

### 影响范围

**后端代码**
- `api/question-router.ts` — 删除 5 个端点：
  - `parseDocument`（解析文档提取文本）
  - `aiGenerateFromFile`（AI 多模态阅读文件出题）
  - `aiGenerateFromDocument`（AI 根据文档文本出题）
  - `importFromDocument`（从文档识别已有题目）
  - `importFromImages`（从图片多模态识别题目）
- `api/lib/document-parser.ts` — 整个文件可删除（Word/PPT/PDF 解析、图片提取）
- `api/lib/ai.ts` — 删除 4 个函数：
  - `generateQuestionsFromDocument`
  - `generateQuestionsFromFile`
  - `recognizeQuestionsFromDocument`
  - `recognizeQuestionsFromImages`

**前端代码**
- `src/pages/Questions.tsx` — 移除文档上传区、图片上传区、导入题目面板相关 UI（约 300+ 行）

**依赖包（可移除）**
- `mammoth` — Word 解析（仅 document-parser.ts 使用）
- `pdf-parse` — PDF 解析（仅 document-parser.ts 使用）
- `pdf-lib` — PDF 操作（仅 document-parser.ts 使用）
- `pngjs` — 图片处理（仅 document-parser.ts 使用）
- `pptx-parser` — 看代码里实际未使用（document-parser.ts 手动用 jszip 解 pptx）

**保留的出题功能**
- `aiGenerate` — 基于知识点内容 AI 出题（不依赖文档上传）
- `submitAnswer` — 答题提交
- `getWrongAnswers` / `markMastered` — 错题本
- `getStats` — 答题统计

### 可选方案

**方案 A：彻底删除（推荐）**
- 删除上述 5 个端点 + document-parser.ts + ai.ts 中 4 个函数
- 前端 Questions 页面移除文档/图片上传 UI
- 从 package.json 移除 `mammoth`、`pdf-parse`、`pdf-lib`、`pngjs`、`pptx-parser`
- **优点**：彻底清理，减少依赖体积和攻击面
- **缺点**：以后恢复需重写

**方案 B：仅隐藏前端**
- 后端接口保留，前端移除 UI 入口
- **优点**：改动最小
- **缺点**：不符合"移除"要求，后端留着死代码和冗余依赖

---

## 三、两个改动的关联

- 无直接依赖关系，可独立执行也可一起执行
- 若一起执行，建议顺序：先移除登录（影响面广，需验证各页面），再移除文档出题（范围集中）

## 四、文档记录需求

- `api/router.ts` 中的 `auth` 和 `question` 子路由会发生变动（auth 可能简化，question 删除 5 个端点）
- `db/schema.ts` 不涉及变动（users/questions 表保留，只是部分功能下线）
- 建议记录：API 端点变更清单、删除的依赖列表、前端路由变更

## 五、Git 提交建议

建议拆分为 2 个 commit：
1. `remove: 移除登录功能（本地运行无需 OAuth）`
2. `remove: 移除文档出题功能及相关依赖`
