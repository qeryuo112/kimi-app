services:
  - type: web
    name: my-app
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run db:migrate && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_SECRET
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: AI_API_BASE_URL
        sync: false
      - key: OWNER_UNION_ID
        sync: false
